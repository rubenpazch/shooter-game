
const history = [];

const gameMolule = (() => {
//  const addNewGame = (objPlayer, objScore) => {
//    history.push({ name: objPlayer.getName(), score: objScore.getScore() });
//  };
//  const getHistory = () => history;
//  return { addNewGame, getHistory };
})();

export default gameMolule;